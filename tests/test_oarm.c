#ifdef HEADERS
#include "../src/oarm.h"
#include "utils.h"
#endif

#ifdef TESTS
/*
TEST("Test oarm formatting") {
  int memory[1024] = {0};
  int result = oarm("hello\ngoodbye\n\t  123 4, 5", memory);
  ASSERT(result, 0);
}*/
#endif